#include <bits/stdc++.h>

int n;
deque<vector<int>> lista = {};

void generarPersona(int n){
    bool genero;

    if(genero == false){
        cout<<"F ";
    }
    if(genero == true)
    for(int i = 0;i<n;i++){
        vector<int> vec;
        lista.push_back(vec);
    }
    if(n )
}

int main(){
    for(int i = 0; i<n;i++){
        generarPersona(n,)
    }
    return 0;
}
